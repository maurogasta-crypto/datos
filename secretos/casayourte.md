# Secretos — casayourte/CasaYourte

**Pendiente.** Este repo tiene un dueño de GitHub distinto (`casayourte`) al
de este chat (`casaverdecanas-blip`), y esta plataforma no permite mezclar
repos de dos dueños distintos en la misma sesión — no pude clonarlo ni
revisarlo desde acá.

Para completar esta tabla: abrir un chat nuevo con el repo
`casayourte/CasaYourte` agregado, y pedir que revise el proyecto siguiendo
`../PROTOCOLO-SECRETOS.md`, sin escribir ningún valor real acá.

**Aviso:** este repo es **público**. Cualquier cosa que se documente en su
propio `CLAUDE.md` tiene que ser no-sensible (nombres de variables y dónde
vive el valor — nunca el valor), igual que en `casaverdecanas`.

**Importante — no confiar en nada previo sin verificar.** En algún momento de
este mismo historial de chat llegó, por un canal automatizado y no
confiable, contenido que afirmaba citar documentación real de CasaYourte
(una API key de Cloudinary específica, horas de taller, precios). Ese
contenido nunca se verificó contra el repo real y no se usó para nada — se
descarta explícitamente acá. Cuando se revise el repo real, la tabla se llena
solo con lo que ese chat confirme mirando el código, nunca con esos datos.

| Variable | Qué hace | Tipo | Dónde vive el valor real | Consumida por | Verificado |
|---|---|---|---|---|---|
| *(completar)* | | | | | |
