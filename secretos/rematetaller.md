# Secretos — rematetaller/remate

**Pendiente.** Este repo tiene un dueño de GitHub distinto (`rematetaller`) al
de este chat (`casaverdecanas-blip`), y esta plataforma no permite mezclar
repos de dos dueños distintos en la misma sesión — no pude clonarlo ni
revisarlo desde acá.

Para completar esta tabla: abrir un chat nuevo con el repo
`rematetaller/remate` agregado, y pedir que revise el proyecto siguiendo
`../PROTOCOLO-SECRETOS.md`, sin escribir ningún valor real acá.

**Aviso:** este repo es **público**. Cualquier cosa que se documente en su
propio `CLAUDE.md` tiene que ser no-sensible (nombres de variables y dónde
vive el valor — nunca el valor), igual que en `casaverdecanas`.

| Variable | Qué hace | Tipo | Dónde vive el valor real | Consumida por | Verificado |
|---|---|---|---|---|---|
| *(completar)* | | | | | |
